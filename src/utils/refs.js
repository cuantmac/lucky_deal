import {createRef} from 'react';

export const navigationRef = createRef();
export const routeRef = createRef();
export const dialogs = {
  giftRef: createRef(),
  bookingRef: createRef(),
  bidFinishRef: createRef(),
  buyBidsRef: createRef(),
  freeBidRef: createRef(),
  getBidsRef: createRef(),
  exitAppRef: createRef(),
  shareRef: createRef(),
  deeplinkRef: createRef(),
  getCouponRef: createRef(),
  clearHistoryRef: createRef(),
  congratulationsDialog: createRef(),
  loadingDialog: createRef(),
  badgeCartDialog: createRef(),
  moreActionRef: createRef(),
  GiftListRef: createRef(),
};
